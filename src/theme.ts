import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: 'rgb(8, 49, 42)',
    },
    secondary: {
      main: 'rgb(5, 29, 25)',
    },
  },
})

export default theme
